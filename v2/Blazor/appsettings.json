{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "OpenCredentials": "Server=(localdb)\\MSSQLLocalDB;Database=OpenCredentials;Trusted_Connection=True;TrustServerCertificate=True;Encrypt=False"
  },
  "OpenCredentials": {
    "_NoticeOverride": "Leave NoticeChannel/NoticeTitle/NoticeBody unset to use the in-code defaults from NoticeService.cs (recommended). Override only if you want a different template than the default."
  }
}
