# Research output — never commit findings (contains repo pointers to
# live leaks; we forward via disclosure channels instead).
findings.json
findings.htm
findings.html
.env

# Defensive: the real settings.json lives in %APPDATA% (outside the
# repo). This catches anyone who drops a copy next to the code.
settings.json
# ...but the Codex SessionStart hook config IS meant to be tracked.
!.claude/settings.json

# Python
*.pyc
__pycache__/
.venv/
venv/

# .NET
bin/
obj/
*.user
*.suo
.vs/
.claude/checkpoint.md
