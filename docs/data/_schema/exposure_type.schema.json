{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mindattic.com/OpenCredentials/schema/exposure_type.json",
  "title": "OpenCredentials ExposureType (L5 canon-as-data)",
  "description": "One broad credential-exposure category. Mirrors the ExposureTypeEntity rows seeded from Patterns.cs ExposureTypes.All; the Findings foreign key points at Name. The auto_inform default gates the auto-notify pass (OC-LAW-2).",
  "type": "object",
  "required": ["codex", "project", "code", "layer", "updated", "entities"],
  "additionalProperties": false,
  "properties": {
    "codex": { "const": 1 },
    "project": { "type": "string" },
    "code": { "type": "string" },
    "layer": { "const": "data" },
    "status": { "type": "string" },
    "updated": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "entities": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "name", "description", "auto_inform_default"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^exposure_type\\.[a-z0-9_]+$",
            "description": "Stable id: exposure_type.<slug>"
          },
          "name": {
            "type": "string",
            "enum": ["ApiKey", "ConnectionString", "PrivateKey", "PlainTextPassword"],
            "description": "Matches ExposureTypes constant in v2/Shared/Patterns.cs and the FK target."
          },
          "description": { "type": "string" },
          "auto_inform_default": {
            "type": "boolean",
            "description": "Seeded default of ExposureTypeEntity.AutoInform. Always false per OC-LAW-2."
          },
          "opt_in_only": {
            "type": "boolean",
            "description": "True if the matching patterns are excluded from the default scan (require --include-passwords)."
          }
        }
      }
    }
  }
}
